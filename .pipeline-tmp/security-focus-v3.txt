VERIFIER HANDOFF (Run 3):
PASS — build 0/0. Targeted fix for Run 2 debugger MEDIUM (edge-state latch).
---
Run 3 diff is minimal — only the rising-edge suppression branches in OnTerminalDataReceived. Both `!armed` and `!outOfCooldown` paths now: log, clear `_emptyDetectorBuffer`, reset `_emptyPlaceholderVisible = false`, early-return. Plus the previously-silent cooldown branch now has a LogTrace (fixes debugger LOW).

NO CHANGES to:
- Chrome anchoring (still placeholder + any one of │/╭/╰/─ in the same 500-byte window)
- Cooldown duration (still 10s)
- Arm delay (still 5s after _claudeDetectedAtUtc)
- Any other detector state

USER DECISION (DEFERRED — do not re-litigate):
The user chose "ship + observe telemetry" for the codex HIGH finding about chrome anchoring being output-only. We will use `_lastClearInputAtUtc` deployment data to decide follow-up. The Run 3 build is intentionally NOT trying to address that finding; it only fixes the latent edge-latch bug introduced in Run 2.

Re-audit specifically:
1. Is the Run 2 edge-latch bug fixed in this diff (yes/no, with code trace)?
2. Does the targeted change introduce ANY new security issue?

Pressure-test only for new defects introduced by this small change. The chrome-anchoring trust-boundary concern is acknowledged and deferred — don't re-flag it. If you have nothing else to flag, approve.

Verdict mapping:
- approve = no new security issues introduced
- needs-attention = a NEW medium/low concern from this specific diff
- block = a NEW critical/high concern from this specific diff
